## 1. Package assets

- [x] 1.1 Extend the existing distribution fixture for authored skills/support files and the companion schema/templates, observe missing-resource RED, implement bounded wheel/sdist inclusion with required YAML/template validation, and verify exact byte parity plus exclusion of unrelated files under GREEN.
- [x] 1.2 Implement the accepted skill catalog/materialization utility after focused RED; verify installed use outside the checkout, editable anchoring, missing payload diagnostics, and support-file availability through the operation context.

## 2. Managed lifecycle

- [x] 2.1 Implement the skill lifecycle owner using pinned zuat.pub calls and separate registry/native-home scopes after observed RED; verify install/inspect/current-no-op/update through real public APIs and exact native file bytes in isolated homes.
- [x] 2.2 Add explicitly filtered history, restoration, and removal after observed RED; verify update A-to-B, process reopen, restoration to A with support bytes and ownership, rejection of a mismatched native home, and recovery of a historical skill absent from the current catalog.
- [x] 2.3 Verify conflict/unowned handling and partial failures with focused failing cases before implementing missing orchestration; prove no-force preservation, supported forced replacement and restoration, exact selected-target isolation, and recoverable failure diagnostics without batch-atomicity claims.

## 3. Project schema lifecycle

- [x] 3.1 Implement packaged schema access and the bounded schema publication plan after focused RED; verify required templates, absent install, identical no-op/adoption, baseline refresh/removal, preserved unrelated files, and pre-write refusal of local edits or redirected targets.
- [x] 3.2 Wire schema publication into normal init/update with validated current baseline storage; verify first init works with schema-dependent OpenSpec discovery, existing-compilation refusal occurs before writes, setup-only/sync preserve schema bytes, all state writers preserve the baseline, races/failures report partial outcomes, and config/change schema selections plus external store roots remain unchanged.

## 4. User interface and integration

- [x] 4.1 Wire the Typer skill group and selection rules after public CLI RED; verify list/status/install/update/history/restore/remove, --json, explicit --agent and --name/--all, --home versus --agent-home, and operation without an OpenSpec project or profile activation.
- [x] 4.2 Update README and relevant Overspec skills with catalog, schema installation/selection/conflict handling, external prerequisites, install/update/restore examples, operation IDs, and state locations; verify referenced commands against help and validate edited skill documents.
- [x] 4.3 Run affected and required broader checks plus an installed-wheel lifecycle smoke in isolated native homes and a clean OpenSpec project; verify packaged schema instructions/templates resolve through OpenSpec without a checkout, project init/update/sync does not mutate native skills, record observed RED/GREEN and limitations, and validate the change before declaring implementation complete.
