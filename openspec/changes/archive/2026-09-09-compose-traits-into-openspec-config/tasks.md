## 1. Discovery and profile activation

For every implementation behavior below, follow the default profile's TDD trait: write and run a focused behavioral test, observe the intended red result, implement minimally, observe green, then refactor under passing tests. Record red/green evidence; do not implement an entire checklist item before establishing its tests. Use the relevant public zuu API before writing equivalent mechanics, recording specific contract mismatches for exceptions.

The profile-toggle revision reopens tasks whose previous implementation followed name-based activation. Checked tasks retain their existing evidence; they do not establish completion of the revised profile contract. Work through user-state task 8.1 before revised selection task 1.2, then the command and lifecycle work. The interrupted activate-NAME draft tests have been replaced; completion evidence for the revised contract is recorded in the implementation report.

- [x] 1.1 Implement project/user profile discovery and recursive trait-file matching using relevant zuu.case5 path contracts; verify default and arbitrary profile names, root/nested trait*.toml files, inactive-profile exclusion, generated-state exclusion, redirected paths, and no duplicate loads. Verify this repository's split default-profile sources load together; use temporary fixtures for loose local traits.
- [x] 1.2 Replace name-based activation with default-only resolution while mode is off and OVERSPEC_PROFILE > saved user selection > default while on. Establish red tests for ignoring dormant selections and unrelated named sources while off, local default overrides, local-only operation without default, enabled environment precedence and invalid-selection errors, and same-name project source shadowing. Remove project selection and invocation profile overrides; verify neither can bypass the mode boundary.
- [x] 1.3 Parse the three declaration arrays with required name/attach/body, optional literal string details, and the sketch's assertion/action fields; retain typed handler payloads for validation through the base contracts in section 3 rather than introducing parser-side behavior dispatch. Verify the revised default sources parse, absent/blank details normalize to absence, non-string details fail, a plain-trait fixture works, custom dotted artifact IDs route literally, and invalid fields/names/attachments fail.
- [x] 1.4 Implement local-over-profile name resolution and deterministic phase ordering; verify complete overrides (including removal of omitted details), cross-type collisions, changed-phase overrides, and source provenance with profile-independent output names.

## 2. Remote profile sources

- [x] 2.1 Add structured public-GitHub profile source registration and an adapter over zuu.case12.GitHubSubpath; verify branch/default/full-commit selection using its injectable public client, explicit unsupported-source errors, and isolation from authored directories.
- [x] 2.2 Implement validated remote revisions and atomic publication pointers; verify download/validation failures preserve the previous usable profile and activation, cache hits still validate content, and local/remote name collisions are reported.
- [x] 2.3 Gate profile pull/update/list/use on enabled mode through the CLI and named-management core entry points. Verify user-only selection persistence, displayed environment override and resolved source, rejected management without side effects while off, unchanged toggle/selection during retrieval, and offline use of a cached default in off mode without inspecting unrelated remote profiles.

## 3. Evaluation and compilation

- [x] 3.1 Define Assertion and Action abstract base classes in their respective base.py files, shared data contracts in trait_system/models.py, and explicit registries in trait_system/registry.py; verify generic dispatch with test implementations, unknown/duplicate registration failures, and imports without registration cycles.
- [x] 3.2 Implement each assertion in its own file under core/assertions/: which.py, files_exist.py, python_dependency.py, require_trait.py, loaded_trait.py, runtime_context_match.py, and runtime_context_includes.py. Use zuu.case5 for confined file inspection and zuu.case13.deep_get for project.dependencies; declare the requirement parser dependency explicitly. Verify through the Assertion contract: both-file AND behavior, missing files/dependency lists, malformed TOML/types/requirements, normalized versioned/extras/direct-reference names, exclusion of similarly named or dev/optional-only packages, read-only context, and explanation results. Establish red before implementing each behavior and review one-concrete-type-per-file placement directly.
- [x] 3.3 Implement RemoveTraitAction in core/actions/remove_trait.py through the Action contract and apply its typed suppression effects in trait_system; verify matching history remains intact, unmatched actions do nothing, and phase/attachment suppression limits are enforced. Review one-concrete-action-per-file placement directly.
- [x] 3.4 Implement generic phase execution, negation, AND/OR, and short-circuiting in trait_system/evaluator.py using the registries without type-specific dispatch branches; verify single-pass ordering, unknown/later-phase errors, the combined-tooling example, and boolean-or-list runtime archive conditions.
- [x] 3.5 Implement scalar body interpolation and explicit variable precedence using zuu.case8 for the layering mechanics; verify missing/non-scalar errors, dollar escaping, reserved-marker rejection, inert command-looking text, and stable names across rendered-body changes.
- [x] 3.6 Implement init/update compilation snapshots and compatibility checks using zuu.case2 source observations and case1 only where its persisted lifecycle fits; verify executable changes wait for update, compile-time source/input/profile changes (including details) require update, ordinary/runtime-only edits do not rerun compilation, read-only checks do not persist baselines, and failed publication preserves the last usable snapshot.
- [x] 3.7 Implement ordinary-trait evaluation at sync/static resolve against retained compiled history; verify fresh ordinary outputs, temporary suppression of compiled bodies, and read-only explanations distinguishing unmatched, overridden, suppressed, retained, and deferred traits.

## 4. Resolution bundles and runtime guidance

- [x] 4.1 Implement immutable root-bound resolution bundles containing all effective declarations, optional details, and static results with content-derived IDs; verify static-only/empty inventories, unmatched/suppressed declaration retention, literal variable-looking details, phase-correct details capture, and equivalent inputs reusing IDs, tampering/missing bundles fail, profile activation changes cannot redirect retained commands, and runtime definitions remain unevaluated at sync.
- [x] 4.2 Implement runtime grouping by exact attachment and resolution; verify one command per group, unique ordered --trait arguments, separate destinations, and correct group positioning and bundled markers.
- [x] 4.3 Add runtime overspec trait resolve with resolution/attachment/trait arguments and --context-file; verify same-group dependency evaluation, only requested matched unsuppressed outputs, invalid IDs/attachment rejection, no recursive command emission, and no writes or network access.
- [x] 4.4 Verify repeated runtime invocations with different or absent context change only their returned guidance, retain short identity, and neither leak invocation state nor modify OpenSpec lifecycle state.

- [x] 4.5 Implement overspec trait show NAME --details with optional --resolution and --project; verify default saved-resolution lookup, explicit historical lookup, full provenance, stale/missing/corrupt/root-mismatched state errors, unknown-name errors, successful absent-details output, and no live-source fallback or assertion/action evaluation. Verify normal resolve/explain excludes details and lookup is offline and read-only.

## 5. Native configuration synchronization

- [x] 5.1 Declare round-trip YAML support directly and wire existing-root YAML/YML selection; verify installation, duplicate-key rejection, invalid mapping errors, YAML precedence, and preservation of custom unowned fields and shared-alias values.
- [x] 5.2 Render static bodies and bundled runtime instructions at attach destinations with short markers, using zuu.case13 for explicit-key traversal where it preserves YAML metadata; verify details are excluded from config and normal runtime output, native string/list shapes, custom artifact IDs, missing/wrong marker repair, and the 50 KiB context limit after command/marker insertion. Document a concrete metadata-contract mismatch if specialized YAML operations are required.
- [x] 5.3 Implement complete owned-field replacement and stale-output removal; verify removed traits/groups disappear, compatible empty inventory clears only owned fields, and unrelated operation properties survive.
- [x] 5.4 Implement read-only dry-run plus value-and-marker-aware no-op detection; verify candidate/diff accuracy, no state publication during preview, and unchanged config bytes/mtime on repeated sync and static-only details edits; verify runtime command references update when their resolution digest changes.
- [x] 5.5 Publish required immutable bundles before atomic config replacement and recheck source inventory/activation/compilation/config inputs with relevant zuu.case5 target plans and case2 observations; verify injected failures never leave config referencing missing state or partially replace config, and temporary YAML files are cleaned up. Publish/reuse a latest-success receipt after config commit or no-op verification; verify owned-value/marker fingerprints, unowned edits remaining compatible, reported resolution IDs, receipt publication failure diagnostics, and safe retry without claiming multi-file atomicity.
- [x] 5.6 Update the packaged lifecycle command surface to remove --profile and obsolete profile activation flags without aliases. Verify common home/project/input options remain usable, removed options fail cleanly, ordinary init/update/sync/resolve work with mode off, and installed-console behavior matches mode-specific help.

## 6. Integration and documentation

- [x] 6.1 Add a companion-OpenSpec integration test using a temporary project: initialize, sync, read proposal/apply/archive instruction JSON, and execute each embedded runtime command; verify correct static inputs, unique bundled commands, conditional returned guidance, and unchanged artifact/lifecycle state.
- [x] 6.2 Revise README/profile examples around activation-free default setup, the user-level mode toggle, enabled user/environment selection, command availability, and local overrides. Preserve the existing lifetime, source, details, runtime, and ownership documentation. Verify off/on examples against the installed CLI in temporary homes/projects without changing sibling repositories.
- [x] 6.3 Verify a temporary project consuming this repository's default-profile directory always includes TDD and includes zuu guidance only when both project files exist and project.dependencies declares zuu; remove each condition in turn and verify the next sync removes stale zuu guidance. Verify compact source bodies, short markers, omitted details in generated guidance, separate full detail lookup, and no skill lookup; compare the eligible case against the seeded guidance before replacing the repository's bootstrap config through a real sync. Record the zuu APIs reused and justified exceptions in the implementation report.
- [x] 6.4 After the profile-toggle delta is implemented, run the complete behavioral suite, companion-OpenSpec integration in both mode states, strict OpenSpec validation, uv lock --check, and uv build. Record red-green evidence, tested OpenSpec/zuu revisions, and packaged off/on command surfaces; update the implementation report so prior completion counts are not presented as verification of the new contract.
- [x] 6.5 Replace argparse with Typer/Rich and separate CLI command modules. Establish failing UX tests first, then verify successful no-argument help, grouped command/option panels, profile tables and empty-state guidance, separate preview sections, complete long redirected lines, common-option inheritance, literal runtime output, undecorated JSON, and concise usage errors; rerun the complete suite and package checks.
- [x] 6.6 Default the user home to ~/.overspec to avoid application collisions; verify profile discovery and activation ignore ~/.over, retain explicit/environment override precedence and project-local openspec/.over paths, and update help and documentation through an observed red-green regression cycle.

## 7. Recursive assertion groups

- [x] 7.1 Establish failing parser tests, then add immutable condition nodes and recursive numbered group parsing through the assertion registry. Verify TOML ownership, per-group boolean or, numeric ordering, leaf arrays, nested groups, malformed/empty/mixed rejection with paths, and compatibility with legacy lists.
- [x] 7.2 Establish failing evaluation and explanation tests, then implement recursive short-circuit AND/OR with leaf negation. Validate every nested reference and phase/attachment restriction. Preserve matched history/actions and flat trace compatibility; expose grouped traces and skipped branches in JSON and terminal explanations.
- [x] 7.3 Verify grouped conditions across compile/update, sync, and retained runtime resolution with red-green tests. Migrate default-profile assertions and README examples without changing bodies/details/actions or condition semantics; retain old resolution readability and unique runtime commands.
- [x] 7.4 Run complete tests, strict OpenSpec validation, lock/build checks, and a real update/preview/sync of this repository followed by a verified no-op. Record the red-green evidence and completed migration in the implementation report.

## 8. Optional profile feature

- [x] 8.1 Replace the interrupted activate-NAME/alias draft with red tests for profile activate as a no-name user-level on/off toggle. Implement persisted profiles.enabled with off as the absent default, preserve profiles.selected and unrelated settings atomically, and report the resulting state. Verify two calls return to off, invalid toggle state fails clearly, selected names survive toggles, and no compile/config/source/network side effects occur.
- [x] 8.2 Build mode-aware Typer command discovery and dispatch per invocation. Establish red tests that only activate appears while off and use/list/pull/update appear only while on; test direct disabled calls, completion, removed aliases/arguments, repeated in-process toggles, and explicit --home in every supported common-option position. Keep ordinary and retained-resolution commands available in both modes.
- [x] 8.3 Observe applicable mode/user/environment selection at preparation and recheck it before compilation/config publication. Verify detected effective-selection changes abort publication, changing the selected source requires update, toggling with the same effective default does not alone invalidate compilation, and saved runtime/detail lookup remains unchanged and read-only after disabling profiles.
- [x] 8.4 Remove obsolete project profile-selection configuration and replace old profile-selection test expectations during implementation; preserve variables, default-profile authoring files, and retained bundles. Verify the repository consumes its default profile with mode off, and document the revised behavior without a compatibility alias or migration layer.
